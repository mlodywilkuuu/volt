// kernel.c

// Funkcja, która zostanie wywołana przez nasz kod asemblerowy
void kernel_main() {
    // 1. Inicjalizacja sterowników (UART, FrameBuffer, itp.)
    
    // 2. Pętla główna systemu (tu będziesz obsługiwać procesy)
    while (1) {
        // Tu będzie scheduler, który zarządza zadaniami
    }
}
